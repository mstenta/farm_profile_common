langcode: en
status: true
dependencies:
  config:
    - farm_profile.profile_type.common
    - field.storage.profile.farm_physical_address
  enforced:
    module:
      - farm_profile_common
  module:
    - address
id: profile.common.farm_physical_address
field_name: farm_physical_address
entity_type: profile
bundle: common
label: 'Farm Physical Address'
description: 'An address for the physical farm, so that the farm can be placed in a regional context.'
required: false
translatable: false
default_value: {  }
default_value_callback: ''
settings:
  available_countries: {  }
  langcode_override: ''
  field_overrides:
    givenName:
      override: hidden
    additionalName:
      override: hidden
    familyName:
      override: hidden
  fields: {  }
field_type: address
